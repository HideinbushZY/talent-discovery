# secrets — never commit real keys（.env / .env.bak / .env.local 等，但保留 .env.example）
.env
.env.*
!.env.example

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# os
.DS_Store

# local editor / tooling config (machine-specific)
.claude/

# runtime cache / 持久化数据
.cache/
data/
